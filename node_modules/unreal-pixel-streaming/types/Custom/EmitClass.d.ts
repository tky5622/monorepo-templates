import { UeDescriptor } from "../UeInstanceMessage/UeDescriptor";
import { IBodyCommand } from './Interface';
declare global {
    interface Window {
        ps_callback_id: number;
    }
}
declare class EmitClass extends UeDescriptor {
    constructor(data: any);
    get verification_id(): string;
    /**
     * Send emit with json
     * @param descriptor - The string descriptor to send
     */
    emit(descriptor: string): void;
    /**
     * Send emit with json
     * @param descriptor - The any descriptor to send
     */
    emitJson(descriptor: any): void;
    /**
       * Send emit with json
       * @param command - The string descriptor to send
       * @param body - The JSON descriptor to send
       */
    emitCommand(command: string, body: JSON, initiator?: string): IBodyCommand;
    emitCommandSystem(command: string, body: JSON): IBodyCommand;
    emitConsole(descriptor: string): void;
}
export default EmitClass;
