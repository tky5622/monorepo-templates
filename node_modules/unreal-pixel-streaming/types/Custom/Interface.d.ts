declare type initiatorTypes = string | 'browser' | 'ue' | 'system';
export interface IBodyCommand {
    command: string;
    verification_id: string;
    initiator: initiatorTypes;
    request: {
        body: JSON;
    };
}
export interface IBodyCallback {
    command: string;
    verification_id: string;
    initiator: initiatorTypes;
    request: {
        body: JSON;
    };
    response: {
        body: JSON;
    };
}
export {};
