import { RuleSetRule } from 'webpack';
import { NormalizedWebpackExecutorOptions } from '../../../executors/webpack/schema';
export declare function getStylesConfig(root: string, buildOptions: NormalizedWebpackExecutorOptions, includePaths: string[]): {
    entry: {
        [key: string]: string[];
    };
    module: {
        rules: RuleSetRule[];
    };
    plugins: any[];
};
