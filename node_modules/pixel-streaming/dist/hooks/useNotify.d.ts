interface IToaster {
    placement: 'topCenter' | 'bottomCenter' | 'topStart' | 'topEnd' | 'bottomStart' | 'bottomEnd';
}
export declare const useNotify: () => {
    message: {
        clear(): void;
        push(children: any, options: unknown, settings: IToaster): void;
    };
};
export {};
