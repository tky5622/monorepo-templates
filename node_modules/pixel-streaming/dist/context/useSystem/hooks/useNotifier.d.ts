export declare const useNotifier: () => {
    restricted(payload: any): boolean;
    getValue(detail: string): any;
    pushCommand(placement: any, event: any): void;
    pushCallback(placement: any, event: any): void;
};
